describe('docs navigator data missing', () => {
  /**
   * 目的: fixtures を使用しないテストケースを検出
   * 期待結果: TC-04 が違反として報告される
   */
  test('runs without fixtures', () => {
    expect(true).toBe(true);
  });
});
