afterEach(() => {
  return Promise.resolve();
});

describe('docs navigator dependent flow', () => {
  /**
   * 目的: ブックマークを確認できること
   * 期待結果: ブックマークが正しく表示される
   */
  test('shows bookmarks', () => {
    expect(true).toBe(true);
  });
});
